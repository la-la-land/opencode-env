# ============================================================
# Honcho v3 — ЛОКАЛЬНЫЙ режим (без облака и API-ключей).
# LLM (deriver/dialectic/summary/dream) → локальная Gemma 4 12B
# через llama.cpp :1234. В контейнерах хост = host.docker.internal.
# configure.sh перегенерирует honcho/.env из stack.config
# (переменные HONCHO_LLM_*), но можно править и вручную.
# ============================================================

# --- auth: локально без токенов ---
AUTH_USE_AUTH=false
LLM_OPENAI_API_KEY=local

# --- эмбеддинги: собственный embedder honcho (контейнер :8100) ---
EMBED_MESSAGES=true
EMBEDDING_VECTOR_DIMENSIONS=384
EMBEDDING_MODEL_CONFIG__TRANSPORT=openai
EMBEDDING_MODEL_CONFIG__MODEL=paraphrase-multilingual-MiniLM-L12-v2
EMBEDDING_MODEL_CONFIG__OVERRIDES__BASE_URL=http://embedding:8100/v1
EMBEDDING_MODEL_CONFIG__OVERRIDES__API_KEY_ENV=EMBEDDING_API_KEY
EMBEDDING_API_KEY=local

# --- локальная LLM (ед.точка настройки) ---
HONCHO_LLM_BASE_URL=http://host.docker.internal:1234/v1
HONCHO_LLM_MODEL=local-coder

# --- deriver ---
DERIVER_MODEL_CONFIG__TRANSPORT=openai
DERIVER_MODEL_CONFIG__MODEL=${HONCHO_LLM_MODEL}
DERIVER_MODEL_CONFIG__OVERRIDES__BASE_URL=${HONCHO_LLM_BASE_URL}
DERIVER_MODEL_CONFIG__OVERRIDES__API_KEY_ENV=LLM_OPENAI_API_KEY

# --- summary ---
SUMMARY_MODEL_CONFIG__TRANSPORT=openai
SUMMARY_MODEL_CONFIG__MODEL=${HONCHO_LLM_MODEL}
SUMMARY_MODEL_CONFIG__OVERRIDES__BASE_URL=${HONCHO_LLM_BASE_URL}
SUMMARY_MODEL_CONFIG__OVERRIDES__API_KEY_ENV=LLM_OPENAI_API_KEY

# --- dream (дедукция/индукция) ---
DREAM_DEDUCTION_MODEL_CONFIG__TRANSPORT=openai
DREAM_DEDUCTION_MODEL_CONFIG__MODEL=${HONCHO_LLM_MODEL}
DREAM_DEDUCTION_MODEL_CONFIG__OVERRIDES__BASE_URL=${HONCHO_LLM_BASE_URL}
DREAM_DEDUCTION_MODEL_CONFIG__OVERRIDES__API_KEY_ENV=LLM_OPENAI_API_KEY

DREAM_INDUCTION_MODEL_CONFIG__TRANSPORT=openai
DREAM_INDUCTION_MODEL_CONFIG__MODEL=${HONCHO_LLM_MODEL}
DREAM_INDUCTION_MODEL_CONFIG__OVERRIDES__BASE_URL=${HONCHO_LLM_BASE_URL}
DREAM_INDUCTION_MODEL_CONFIG__OVERRIDES__API_KEY_ENV=LLM_OPENAI_API_KEY

# --- dialectic (уровни скрупулёзности) ---
DIALECTIC_LEVELS__minimal__MODEL_CONFIG__TRANSPORT=openai
DIALECTIC_LEVELS__minimal__MODEL_CONFIG__MODEL=${HONCHO_LLM_MODEL}
DIALECTIC_LEVELS__minimal__MODEL_CONFIG__OVERRIDES__BASE_URL=${HONCHO_LLM_BASE_URL}
DIALECTIC_LEVELS__minimal__MODEL_CONFIG__OVERRIDES__API_KEY_ENV=LLM_OPENAI_API_KEY

DIALECTIC_LEVELS__low__MODEL_CONFIG__TRANSPORT=openai
DIALECTIC_LEVELS__low__MODEL_CONFIG__MODEL=${HONCHO_LLM_MODEL}
DIALECTIC_LEVELS__low__MODEL_CONFIG__OVERRIDES__BASE_URL=${HONCHO_LLM_BASE_URL}
DIALECTIC_LEVELS__low__MODEL_CONFIG__OVERRIDES__API_KEY_ENV=LLM_OPENAI_API_KEY

DIALECTIC_LEVELS__medium__MODEL_CONFIG__TRANSPORT=openai
DIALECTIC_LEVELS__medium__MODEL_CONFIG__MODEL=${HONCHO_LLM_MODEL}
DIALECTIC_LEVELS__medium__MODEL_CONFIG__OVERRIDES__BASE_URL=${HONCHO_LLM_BASE_URL}
DIALECTIC_LEVELS__medium__MODEL_CONFIG__OVERRIDES__API_KEY_ENV=LLM_OPENAI_API_KEY

DIALECTIC_LEVELS__high__MODEL_CONFIG__TRANSPORT=openai
DIALECTIC_LEVELS__high__MODEL_CONFIG__MODEL=${HONCHO_LLM_MODEL}
DIALECTIC_LEVELS__high__MODEL_CONFIG__OVERRIDES__BASE_URL=${HONCHO_LLM_BASE_URL}
DIALECTIC_LEVELS__high__MODEL_CONFIG__OVERRIDES__API_KEY_ENV=LLM_OPENAI_API_KEY

DIALECTIC_LEVELS__max__MODEL_CONFIG__TRANSPORT=openai
DIALECTIC_LEVELS__max__MODEL_CONFIG__MODEL=${HONCHO_LLM_MODEL}
DIALECTIC_LEVELS__max__MODEL_CONFIG__OVERRIDES__BASE_URL=${HONCHO_LLM_BASE_URL}
DIALECTIC_LEVELS__max__MODEL_CONFIG__OVERRIDES__API_KEY_ENV=LLM_OPENAI_API_KEY

# --- прочее ---
CACHE_ENABLED=true
CACHE_URL=redis://redis:6379/0?suppress=true
VECTOR_STORE_TYPE=postgres
SENTRY_ENABLED=false
METRICS_ENABLED=false
TELEMETRY_ENABLED=false